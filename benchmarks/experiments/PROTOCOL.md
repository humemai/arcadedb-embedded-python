# PROTOCOL

How this benchmark harness must be operated. Every rule carries a tag saying what enforces it:

- **GATE** `<name>`: something refuses. Either one of the four gate scripts (`provenance_check`, `fairness_check`, `page_check`, and `equivalence_check` from the 2026-10 instrument, run by `refresh_web_page.py`) or a refusal in the runner or a lane at production time.
- **HAND-RUN** `claims_check`: checked only when someone runs `claims_check.py`. It is a helper library that `page_check` imports, not a gate.
- **REMEMBERED**: nothing will catch the next violation.

Companion documents: FAIRNESS.md (F1 to F12, when two numbers may be compared), PAGE-SPEC.md (what the page contains), PUBLISHING.md (how it is published), READING-RESULTS.md (how to read `results/`), CAMPAIGN.md (how a campaign is run), COMPARATORS.md (what each comparator is pinned to). The canonical-row definition is section 1 below. There is no paper; the page is the only publication target, and the generated LaTeX tables T2 to T5 survive as the artifact `page_check` pins the page against.

---

## 1. What may reach a table

The admission filter is `make_paper_tables.load_canonical()`. Every test below runs **before** the dedupe, so a bad row can never shadow a good one by being newer. A row that reaches a cell through an overlay directory never passes the filter at all; the tags say where that matters.

- `rc == 0`. **GATE** `load_canonical`
- Scale is in `PAPER_SCALES` for that lane. Retired and exploratory tiers cannot reach a cell. **GATE** `load_canonical`
- `cpuset` is `"0-11"` or unrecorded. A partial cpuset is a parallel sweep shard, and the dedupe keys on `ts_utc` with no idea what a cpuset is. **GATE** `load_canonical` + `fairness_check.check_cpuset` (F1/F2, vacuous downstream of the loader)
- `engine_version` matches no `dev\d|SNAPSHOT`. **GATE** `load_canonical._DEV_RE` for T2, T3 and the comparator rows; **GATE (report only)** for T5's time-series row, which warns on stderr and writes the row anyway; **REMEMBERED** for T5 dense, which never passes through the loader
- l3s rows carry `recall_at_10`. The synthetic sparse generator ships no ground truth, and at tiny and small both corpora hold the same document count, so nothing else tells them apart. **GATE** `load_canonical`
- Elasticsearch rows carry `server_heap` equal to `heap`. **GATE** `load_canonical` + `runner.observe_server`
- Elasticsearch rows carry `es_prune is False`. ES 9.1+ prunes by default on thresholds tuned for ELSERv2; on our SPLADE corpus that is recall 0.725-0.929 against 0.991-0.9985. **GATE** `load_canonical`
- A served row names a `d.d.d` version or carries an `@sha256` digest. `server:latest`, `server` and `unknown (PackageNotFoundError)` are not versions and the pre-release guard cannot see through them. **GATE** `load_canonical` for `runs.jsonl` rows; **GATE** `_engine_is_identifiable` in `export_web`, which withholds an ArcadeDB row whose engine string and commit identify no build
- A witness field must be present **and** equal, because bare equality passes by matching `None` against `None`. **GATE** `load_canonical`
- Dedupe key is `(lane, scale, n_docs, workload, backend, gav is not False, rep)`, latest `ts_utc`, never `run_id`. Without the `gav` term the no-view ablation overwrites T3. **GATE** `load_canonical`
- One released engine line per table. Delete preference cascades, never reorder them. **GATE** T4 refusal in `make_paper_tables` and five of f8's six bars in `make_paper_figures`; **REMEMBERED** for f8's dense bar, whose `_dense_multipass` source reads no version field, and for every other table (F5)
- Lane scripts publish; bespoke drivers investigate. Both known protocol violations were bespoke-driver rows promoted to cells, and T5's dense half is still an overlay from `dense_multipass_driver.py`, run through `runner.py --driver`. **GATE** `fairness_check.report_producers` (F6b) for `runs.jsonl` lanes, which sees no overlay record; **REMEMBERED** for every overlay-fed cell
- Every cell is N=5, or its shortfall is disclosed in `EXPECTED` and in the caption, and the disclosure is deleted when the shortfall is fixed. **GATE** `provenance_check.caption_n`, inert while `BENCH_PAPER_DIR` is unset; **REMEMBERED** for T5's dense half and for the deletion half, which `if ns == {5}: continue` short-circuits
- Never publish a scale where comparators have rows and we do not; print what was withheld and why at export time. **GATE** withheld-scale guard in `export_web`
- Every artifact the page cites lives in this repository, and the superseded run is not left where it can be compared against. **GATE (report only)** `provenance_check.FEEDS`: an empty feed prints "(no result files)" and counts nothing. **GATE**: every pinned reader refuses when `<name>_<pin>` is missing
- Every overlay directory and top-level result file feeding a table records run conditions and a real version; "feeds a table" means "is opened by `make_paper_tables`". **GATE** `run_conditions`, `lane_files` and `_is_real_version` for the two lane files; **REMEMBERED** for the overlay directories T4 and T5 print, whose audit accepts any non-empty string
- A SNAPSHOT build's commit date must not predate a landmark commit. **GATE** `provenance_check.LANDMARKS`

## 2. How a cell is measured

- Published latency, throughput, percentile and memory cells run serial, one at a time, on the full cpuset. Permutation protects a ratio; it cannot restore an absolute level or a tail that never happened. **GATE** `runner.py` paper tier refuses `workers != 1`
- Every row names the instrument it was measured under (`instrument`, from `bench_common.INSTRUMENT`; rows before 2026-10 carry none and are the September instrument) and the machine (`bench_host`, refused at paper tier when unset). Rows from two instruments never share a table: a five-query analytics row beside a two-query one, or a relaxed-durability write beside an fsync one, is not a comparison. **GATE** `load_canonical`, `export_web._instrument_of`, `fairness_check.check_durability`
- The 2026-10 query set is forty operations, settled and closed to additions (DECISIONS #82d), each one written once per engine in its own language and smoked on the laptop through the runner before the chain starts: document OLTP 6 (TPC-C new-order and payment, and one record inserted, read by key, updated, and deleted), document OLAP 5 (TPC-H Q1 and Q6, top ten parts by revenue, count by ship mode, and revenue by month, none of which needs a join), graph OLTP 7 (point lookup, 1-hop, 2-hop, 3-hop with a property filter, insert, update, and delete), graph OLAP 5 (average friend age, friendships within a city, most friends, degree distribution, and triangle count), dense vectors 3 (search, insert into a built index then search, and delete from a built index then search, the last two at the 1M tier only), sparse vectors 1 (search, at three sizes), time series 5 (newest reading, twelve-hour aggregate, double group-by, high-usage filter, and group-by with ordering and limit), cross-model 4 (the composed transaction, the same interrupted, retrieval, and graph-filtered vector search), and lifecycle 4 (open, one query, one write, and close). Eighteen of the forty come from a published benchmark. Repetitions stay at five everywhere, with 1,000 transactional operations per repetition, 100 runs per analytical query per repetition, and 1,000 queries per vector pass. **REMEMBERED** (F4)
- Every table reports one measurement set (DECISIONS #89): cold and warm latency at the median and the ninety-ninth percentile, where the first iteration after the database is opened is the cold number and the rest are the warm one, throughput where the operation has a natural rate, recall where the index is approximate, peak memory, on-disk size after the workload, and on the vector tables ingest and index build as separate timers. A table that omits one of these states why in a condition rather than leaving a blank: a transactional cell runs against a warm database by construction, and the lifecycle table is itself the cold measurement. **GATE** `page_check`
- Every timed query whose answer is deterministic records a canonical digest of that answer, order-insensitive unless the query defines an order, floats rounded to a fixed precision before hashing, with a small readable sample kept beside the digest so a disagreement can be read and not only detected. A query an engine cannot express is declared absent in its adapter, never silently skipped. **GATE** `equivalence_check`, which refuses a table whose engines disagree at one scale and names every declared absence (DECISIONS #88). The vector lanes keep recall against ground truth and the cross-model lane keeps its torn-state comparison, both stronger than a digest over an approximate answer.
- A summed or averaged measure is declared numeric in its query's digest spec and compared as a number whatever type the driver returns, while counts and identifiers stay exact. ArangoDB's AQL `SUM` over integer-stored values returns an integer where every other engine returns a double, which split the pricing summary seven engines to one at SF1 on an answer nobody had wrong, and a count rounded to six significant digits would let 1,234,567 and 1,234,568 agree. The declaration is per query and never per engine, so it cannot be used to make one engine's answer match another's. **GATE** `equivalence_check` over `bench_common.canonical_rows`
- The revenue total and the revenue by month count the rows they aggregate, and that count is compared exactly, because at the campaign's largest tier one lost row moves each sum by less than the digest's rounding and was detected only 10.8 and 36.5 per cent of the time (DECISIONS #94). **GATE** `equivalence_check`
- ArcadeDB SQL in this repository compares a numeric column through `BETWEEN` or a bound parameter, never against a bare decimal literal, which the engine parses at single precision so that the comparison loses the boundary value (upstream #7609, BUGS.md F42). **GATE** `equivalence_check`
- A served query that projects a temporal value is checked against its embedded twin, because the HTTP serializer prints a time bucket as a date and every bucket inside one calendar day collapses to one value (upstream #7610, BUGS.md F44); the served native time-series group-by is withheld from the page and printed as a known disagreement rather than published beside an answer it does not match. **GATE** `equivalence_check.KNOWN_DISAGREEMENTS` and `export_web.WITHHELD_CELLS`
- **Three different things stop work, and they are not interchangeable.** A query **budget** stops one query; a whole-cell **cap** stops a cell; **abandonment** stops a query early when its budget cannot buy a useful measurement. Each is a property of the work, never of the engine (#85a), so no engine is ever given more room than the engine beside it.
- **Every cell has a time cap, and a cell that exceeds it is censored with the cap named.** The cap is a property of the tier, the same for every engine on it (`runner.TIMEOUT_BY_SCALE`), sized from the measured build at the tier below with headroom and then bounded so the matrix fits: the campaign runs about nine engines at five repetitions across several lanes, and an uncapped slow engine costs days to produce a row that says no more than a censored cell does. When the first repetition leaves no clean row the stage skips the rest, because repeating a timeout measures nothing; the table then shows one censored cell, not five. **GATE** `runner` (the cap), **REMEMBERED** the stage script (the skip). DECISIONS #105
- **A query's budget is derived per lane, per tier and per query, from the bench host's own frozen rows.** It is three times the MEDIAN engine's measured p50 for that query at that tier, times the lane's iteration count: the median because the slowest engine is exactly what a budget exists to bound, and the factor of three because it is the one judgement in the formula and it is the same everywhere. `derive_budgets.py` writes the table into `budgets.py` from `results/runs_paper.csv` and never from a laptop, whose loaded and throttled timings would hand out budgets nobody needs; `budget_lookup.budget_for()` reads it. Where a tier has no measurement the lookup falls back to the lane's old flat constant and stamps `<query>_budget_source` on the row saying so, so a projection can never be mistaken on the page for a measurement. A budget may vary by lane, tier and query, because those are properties of the work; it may never vary by engine. A query that exceeds it stops at the iteration it reached, its numbers are over those iterations, and the cell's other queries keep theirs. **GATE** `budget_lookup` (October instrument; September's lanes carry the flat constants). DECISIONS #106
- **A query whose budget cannot buy five iterations is abandoned after its cold pass.** The cold pass is already paid for, so it is kept and reported; `budget_lookup.abandon(cold_s, budget_s, iterations)` then asks whether the budget buys at least five iterations at the cold pass's own rate, five because a p50 over fewer is not a median of anything. When it does not, the row carries `<query>_abandoned` with the reason in seconds rather than the cell burning hours to prove what the first touch already showed. The test uses only the engine's own cold pass, so it is a property of the work rather than of who is running it. The other queries in the cell are unaffected. **GATE** `budget_lookup.abandon`, called by `l1_tpc`, `l2_graph` and `l4_tsbs` (October instrument); `page_check` allows `\w+_abandoned` as a recorded field. DECISIONS #107
- An indexed lower bound is not assumed to return a whole run of equal entries: an ascending range seek starts where the binary search landed inside the run and loses `floor((k-1)/2)` of the k entries at the bound (upstream #7611, BUGS.md F46), which is why the revenue query disagreed only at the size where the loss landed on a qualifying row. **GATE** `equivalence_check`
- What answer checking cannot prove: DuckDB, PostgreSQL, and SQLite share a SQL dialect closely enough that a defect in a shape all three spell the same way would agree with itself, so the independent evidence comes from the engines with their own languages, MongoDB, SurrealDB, and ArangoDB on the document lane and Neo4j and LadybugDB on the graph lane. ArcadeDB collects the most findings partly because it is the engine examined hardest here, with its source, two deployments, and hand-written SQL. **REMEMBERED**
- Reps per build, warmup count, settle step and query set are properties of the lane, fixed before a driver is written. **REMEMBERED** (F4)
- Never print one-build-many-passes beside per-rep-builds in one table: the two shapes price different work and the table does not show which shape a row has. **GATE (report only)** `fairness_check.check_protocol_overlays` (F4)
- Warm queries must be disjoint from cold ones. Replaying one set cannot separate residency from recall of the same queries. **GATE** `sparse_multipass_driver` protocol for l3s; **REMEMBERED** and knowingly violated for l3d, whose driver replays the `test` set on every pass after 20 untimed warmups drawn from it
- Each engine gets its own vendor-documented settle step, timed inside build so the build is timed to a queryable index, and none is skipped. **REMEMBERED**
- Ingest timers must stop on the same completion semantics across a lane, or the rates price different work. **REMEMBERED**: a new time-series adapter must stop its timer at the same semantics its lane siblings do
- The dense and sparse lanes record `ingest_s` and `index_s` separately wherever the engine has the boundary (ArcadeDB, pgvector, Neo4j, Milvus, and LanceDB), additive, with `build_s` unchanged (DECISIONS #74 item 2). Engines that index while ingesting record neither and carry the total with that as the stated reason. The 2026-10 page prints the two as separate columns; the September page keeps one ingest+index pair. **REMEMBERED**
- Observe conditions from the daemon and the engine; never record what the launcher meant to do. A heap that disagrees with the request fails the cell. **GATE** `runner.observe_server`
- Overlay drivers sample their own peak memory from inside the container, started before the build. `run_conditions` records the ceiling, not the use. **GATE** `bench_common.SelfMemorySampler`
- The cgroup memory reading is valid only inside a bench container; record `host` so a reader knows which they have. **REMEMBERED**
- Time every phase of a lane and record the sum beside the runner's wall clock, so the residual is visible. **GATE** `phases_accounted_s` in `l3_sparse.py`
- Every lane prints `PHASE` markers as it runs (`bench_common.PhaseBeat`), and on a timeout the runner records `timeout_client_disk_mb` and `timeout_cpu_mem`, so a cell that dies inside its budget still says which phase it was in (BUGS.md F41). **GATE** `runner.py` timeout path
- On-disk size is the writable layer plus every declared volume, destinations read from the daemon, and it must settle (two readings within 1%, else `disk_settled=False` with both). A volume in an image with no `du` is sized from a helper container (BUGS.md F38). **GATE** `runner.container_disk`
- A served row's page value is the server container's growth alone; `disk_data_mb` on the row keeps server plus client for the record. **GATE** `export_web._disk_data`
- Record IO alongside disk, taking the last `io.stat` reading: the counters are cumulative. **GATE** `CgroupSampler`
- A silently killed cell must not read as success. **GATE** OOM inspect + non-zero exit in `runner.py`
- Prefer the loud failure: when a lane can measure something other than what was asked for, make it abort rather than annotate. **REMEMBERED**
- A server adapter that sends vectors as text must round-trip float32 exactly (`%.9f`, not `%.6f`). **REMEMBERED**: `precision_check.py` is a synthetic experiment that reads no artifact and nothing invokes it
- Never let a result object's Python type stand in for the finding, and abort an arm whose control operations moved nothing. **GATE** validity guard in `e2_hybrid.py`
- A guard that asks a driver object whether it has an attribute is not a test for absence when the object answers every name: a pymongo `Database` returns a `Collection` for any attribute, so `getattr(client, "reconnects", None)` stamped a `Collection` onto the row and killed every MongoDB document cell at its final write, after the load and after every query (BUGS.md F47). **REMEMBERED**, with a regression test on the October branch
- Pin trial counts and denominators, not only outcomes. "5 of 5" bounds the failure rate only below ~45%; 200 injections bound it at ~1.5%. **HAND-RUN** `claims_check.e2_atomicity`; **GATE** `page_check` count assertions
- Re-profile on the release rather than assuming a dev build's profile transfers, and never add overlapping percentages. **REMEMBERED**
- Do not add a new recorded field to a published driver until its lane is re-run; the driver's own frozen artifacts will not have the field. **GATE** `provenance_check.check_schema_homogeneity`, which compares the measured fields of every row in a (lane, scale, workload, backend) family and reports any field some rows carry and their siblings do not

## 3. How a comparison is kept fair

- Every container in a published cell gets the full 0-11 cpuset; a client/server pair shares that one cpuset rather than each getting its own cores, so CPU competition stays inside the deployment under test. **GATE** `fairness_check.check_cpuset` (F1) + the loader filter, for the client or embedded container only; **REMEMBERED** for the server side of a pair, whose `server_cpuset` no consumer reads
- Every backend at a (lane, scale) gets the same `--memory`/`--memory-swap` and, for JVM engines, the same heap. Raising a resource for one engine obliges a re-measure of every engine at that tier. **GATE** `fairness_check.check_envelope` (F3)
- Normalize a served topology before comparing: total is `srv_cap / mem_split`, never client + server. **GATE** `_total_envelope` in `fairness_check`
- Do not compare a setting to its own absence: heaps are compared only among engines that have one, and a missing heap on an ArcadeDB row is reported as a recording gap. **GATE** `check_envelope`
- Every dense backend at a scale gets the same effective base-layer degree, read in each engine's own unit (ArcadeDB `maxConnections` as-is, hnswlib-family `M` doubled at layer 0). A row recording no degree FAILS; sqlite-vec is exempt as an exact scan, and an index with no degree at all is matched by effect instead (F7). Stamp the matched parameter from one shared function, called from both the lane and its bespoke driver. **GATE** `fairness_check.check_degree` (F7) + `l3d_dense.degree_stamp(backend)`
- Check the rows the table prints. Where a table reads an overlay, the fairness checks read that overlay too. **GATE** `_dense_rows()` swap in F3/F7
- Fit every engine's thread pools to the cpuset via `sched_getaffinity`; `os.cpu_count()`, `nproc --all` and `/proc/cpuinfo` in an adapter are the bug. **REMEMBERED** (F6, and three DuckDB lanes are still unfixed)
- Verify the shared cpuset equalizes **use**, not merely the resource, before comparing p50s. **REMEMBERED** (F8 holds the measurement)
- When a campaign re-measures one engine and carries the others forward, re-run one untouched comparator as a control and record its old-vs-new delta beside the table it licenses. If the control does not reproduce, the tier is re-measured in full. **REMEMBERED** (F9)
- Measure every row in one table on the same engine release as the row beside it, so a version difference cannot be read as a configuration difference. **GATE** T4 and f8 only; **REMEMBERED** elsewhere (F5)
- Never divide a number from one experiment by a number from another and call the quotient a cost. **HAND-RUN** `claims_check._tentag` and the dense cold/warm boundary note
- Equalize a comparator's default only where leaving it makes the comparison apples-to-oranges (the four categories in section 7). **REMEMBERED**
- A durability default is relaxed only as a matched class over the whole table, never for one arm: from 2026-10 every engine that has the setting commits without waiting for the disk, each engine's setting is read out of the engine rather than assumed, and the row records what it ran as `durability` (DECISIONS #81). **GATE** `fairness_check.check_durability` (F10)
- Every timed write runs at both durability settings and the row records which one it ran; ingest runs at one setting on every lane, because a bulk load commits in batches, the flush is amortised, and a second build buys nothing (DECISIONS #90, #90a). Whether our loaders commit per batch rather than per row is settled once by a laptop check, never by a campaign axis. **GATE** `fairness_check.check_durability` (F10)
- The strict setting is a fixed per-commit tax set by the device, so every engine lands in the same narrow band when it waits and the ratio between the two settings is inversely proportional to how fast that engine's relaxed path is. It follows that a published relaxed write measures how well an engine avoids the disk as much as how fast it writes, and that under strict durability the engines converge; the framing cuts against us as much as for us, since ArcadeDB embedded's fast relaxed document insert is what makes its own multiple one of the largest. **REMEMBERED**
- A fix that costs the rival a round-trip goes only in the untimed path, or the round-trip is charged to their latency. **REMEMBERED**
- State the consequence of the memory contract that cuts against us: the JVM heap lives inside the container cap, so ArcadeDB's 24 GiB heap leaves about 12 GiB for JVM overhead and page cache while non-JVM comparators have all 36 GiB. Equal caps are what we can enforce. **REMEMBERED**
- Keep `fairness_check.DISCLOSED` empty unless an override is live, and delete a disclosure the moment the asymmetry it describes is fixed. **REMEMBERED**

## 4. What may be said about a number

- Never recompute a claim your own way: `import make_paper_tables` and ask `load_canonical()`. **HAND-RUN** `claims_check`
- Pin prose to a generated table cell (prose to table to data), address cells by meaning (system, tier, metric) and never by column index, and parse the k/M suffixes when reading a cell back. **HAND-RUN** `claims_check.sparse_cell`, `cell()`
- Verify separately that the committed tables regenerate byte-identically from the data (generate into a temp dir and diff). Otherwise every cell-pinned claim passes against a stale file. **HAND-RUN** `claims_check --regen`
- Tolerance is the number's own printed rounding, half a unit in the last digit, so a wider window cannot pass a number the table no longer prints. **GATE** `page_check._check_prose`, which derives it as `0.5 * 10 ** -decimals`; **REMEMBERED** for `claims_check`, where tolerance is a hand-typed third tuple element and several are an order of magnitude past half a unit
- A selector that returns no rows, or raises, is a FAILURE, never a skip. **HAND-RUN** `claims_check` NODATA/ERROR paths
- Pin ranks as well as values, and fail loudly when a name stops resolving; never let a lookup fall back to a sentinel. **HAND-RUN** `_dense_rank` raises on unresolved `DENSE_ROWS`
- Pin a ratio by recomputing it from its operands, and pin the endpoints of any range stated as one summary number. Both operands can be right and the quotient stale. **HAND-RUN** `claims_check` ratio selectors
- State every ratio in one direction, ours over theirs, even where flipping it would read as a win, so a regression moves the whole group the same way. **HAND-RUN** `mem.ratio.neo4j`
- Pin a "beats both" claim to the comparator we lose to. **HAND-RUN** `claims_check` L2 block
- When one system contributes two arms, pin each arm separately. **HAND-RUN** `claims_check` l4 arm claims
- Select on `n_docs` wherever one scale name covers two corpora, and count reps for one arm, because an N claim describes a cell and not a pooled lane. **HAND-RUN** `_sparse_build`, `_sparse_reps`
- Exclude the GAV ablation (`gav is False`) from every ordinary selector; the ablation gets its own reader. **HAND-RUN** `claims_check._sel`
- Read one source per measurement. No fallback cascade across engine lines or overlay generations: the arms use different last-point keys, so a fallback changes the quantity as well as the engine. **HAND-RUN** `ts_arm`
- A number that appears in no table needs its own claim, or it has no auditor. **HAND-RUN** `gav_ablation`, `ingest_ab`, `e3`, `_sparse_arm`, `l2_peak_anon_gib`
- Leave an unsourced prose number pinned and FAILING rather than deleting the claim or nudging the number. **REMEMBERED**
- Pin a known confound as a checkable quantity instead of publishing the ratio it corrupts. Never publish a cross-engine memory ratio computed from anonymous memory: anon is 26% of PostgreSQL's peak and 97.5% of ArcadeDB's. **HAND-RUN** `anon_share`, `anon_client_share`
- Split a served cell's memory into client and server before comparing with an embedded engine. **HAND-RUN** `mem.postgres.client_share`
- Reject a throughput from a run that wrote the wrong number of rows. **HAND-RUN** `ingest_ab` honors `count_ok`
- Aggregate a decomposition over all matched released runs, and pin the fact that a term sits below resolution rather than quoting one tidy run. **HAND-RUN** `_e4_decomp`
- Pin a binding-overhead ratio by the exact pair of arms it compares. **HAND-RUN** `pyb_ratio`
- Sweep for ratios and unit-bearing measurements that no claim pins, so a new number cannot reach a freeze without an auditor. **HAND-RUN** `claims_check --sweep`
- Every generated figure must be newer than the newest generated table, and no figure may exist that nothing cites. **GATE** `figures_fresh`, `_check_no_orphan_figures`
- Both sides of a plotted ratio come from one artifact at one pass selection. **GATE** `_check_f4_protocol`
- Enforce a comparator-selection rule in code, not on the axis: the real rule is "fastest engine at recall at least ours". **GATE** `_check_f4_comparators`
- Read labels back out of the rendered PDF. A figure can be wrong while every data check is right. **GATE** `_check_labels_intact` + `EXPECT_IN_PDF`
- A figure label must carry its own valence and must not borrow another experiment's vocabulary. **REMEMBERED**
- A caption's asserted engine version must appear in the versions its feeding overlays record; distinguish BAD (data contradicts) from UNVERIFIABLE (data cannot say). **GATE** `provenance_check.caption_versions`, inert while `BENCH_PAPER_DIR` is unset
- Flag a table whose rows come from more than one engine version, and name every driver that writes a hardcoded version literal about itself. **GATE (report only)** `provenance_check`
- The page may show fewer numbers than the generated tables, never a different one: a page cell covering a claimed measurement must agree within that claim's tolerance, a STALE mapping fails, an ABSENT cell fails, and a pin for a deliberately dropped table is deleted rather than left reporting ABSENT. **GATE** `page_check.MAPPING`; deletion is **REMEMBERED**
- Key page pins on the raw harness backend name and resolve through the exporter's own `display_name()`, literal key first. **GATE** `page_check._resolve`
- Pin every hand-typed number in the page's prose to a named table column, keep cold and warm entries listed apart, and treat an unmatched regex or an unparseable capture as a failure. **GATE** `page_check.PROSE`
- Check the page's atomicity counts against the artifact and assert the trial count alongside every zero-torn claim, because zero is what a broken read also produces. **GATE** `_check_page_atomicity`
- After copying the export to the site, verify the served file is byte-identical to the one the gates read. **GATE** `refresh_web_page`
- Read every published label from the thing it describes: deployment from the backend topology, version from the image tag, scale from an explicit map, dense precision from the adapter. **GATE** `export_web` topology lookup and `SCALE_LABELS` for l1/l2/l3; **REMEMBERED** for the adapter half, since `DENSE_PRECISION` is a hand-transcribed dict nothing compares against `l3d_dense.py`, and for l4, which bypasses `deployment_of()` for a hardcoded `L4_DEPLOYMENT`
- Give every arm a display name; a raw backend id names a harness arm, not a system. **GATE** `display_name()`
- A table may have more than one source, and the source link printed under it must hold the rows above it. **REMEMBERED**: `SOURCES` is hand-maintained, and a table id missing from it gets an empty list and no warning
- State a comparator's storage precision from its own version-pinned source, never from our recall or a doc page for another version. **REMEMBERED**

## 5. How the checkers themselves must behave

- A check that could not run is carried to the summary as NOT CHECKED, not as a pass. **GATE** `_CAPTION_CHECK_RAN` and its warning, and `page_check._check_prose` returning `(0, 1)`; **REMEMBERED** for the rest, which is most of them
- Handle both artifact shapes (object and array of per-pass records), and count an unreadable file as a finding rather than skipping it. **GATE** `_versions_in`, `run_conditions`
- Search for a version at every nesting level, since a key nested under `run_conditions` is invisible to a top-level scan, and reject populated but uninformative values ("unknown...", "?", "n/a", anything with no digit). **GATE** `_nested_version_keys` and `_is_real_version`, neither of which the overlay audit calls
- Exclude sidecars (`_buildstats.json`, `_gc.json`, `_manifest.json`) from version and condition audits. A checker that cries wolf is a checker nobody runs. **GATE** `SIDECAR_SUFFIXES`
- A gate that fails on compliance is worse than no gate: fix the checker before believing its findings. **REMEMBERED**
- Derive a reported quantity by instrumenting the real generator, not by re-deriving it. A second implementation is a second thing that can drift. **GATE** `caption_n` monkeypatches `mmm`/`mmm_rec`
- Count unfixable history rather than failing on it, and name what it is. **GATE** counters in `provenance_check` and `fairness_check`
- Keep check functions and their tables above `if __name__ == "__main__"`, and key them on the lane spelling `runs.jsonl` uses. **REMEMBERED**, nothing tests that a check is reachable

## 6. How work is queued and stored

- Every campaign dataset switch lives in `campaign_env.sh` in the repo. The October queue scripts source it; the September scripts set the same variables inline. A launcher in a home directory is not where configuration lives. **REMEMBERED**: nothing sources the file, and the live September scripts hardcode their own switches
- Check each corpus is present, not merely that its path variable is set; a set path with no corpus is how the synthetic sparse rows got in. **GATE** `campaign_env_check()` for a caller that sources it
- Run a smoke stage before any long matrix and let it abort the campaign. **REMEMBERED**
- One runner per bench host. `sweep_orphans()` destroys a live campaign's in-flight cells, so the runner takes an exclusive flock; never rebuild a bench image and never co-run a smoke while a campaign is live. **GATE** `results/.runner.lock`
- The env forward list is an allowlist and gets a new entry with every new switch, or the flag parses and changes nothing; naming a workload no selected lane defines is an error. **GATE** allowlist and explicit-workload validation in `runner.py`
- A new tier is registered only when every scale table has it. **REMEMBERED**, argparse validates against the first table only
- Pin server images by immutable digest and client libraries by exact version; the embedded wheel and the server image must be the same release. **GATE** partial (served rows must carry a version or digest); **REMEMBERED** for the client pins
- Read a library version from the installed distribution (`importlib.metadata`), never from a module attribute, and name the engine core rather than the SDK that wraps it (BUGS.md F39). **REMEMBERED**: no check inspects how a version was obtained
- Merge campaign rows by appending, never replacing, and refuse the merge if it would shrink the canonical cell count. Dry run by default, timestamped backup first. **GATE** `merge_campaign.py`
- Keep the live append log untracked and write `results/runs_paper.csv` in the same call that writes the tables, so published rows and published tables cannot come from different states of the log. **GATE** `make_paper_tables.main`
- Archive before deleting anything under `results/`, guard the delete on the archive's own contents, and keep the keep-list covering the logs of every job it keeps, not just the scripts. **REMEMBERED**
- Read the artifact for current state. Trackers, task notes and append-only status files are history; only the last entry describes now. **REMEMBERED**
- Published numbers are measured on tk@mini; a number from another host is not comparable with the rows beside it. The laptop is for harness development and smoke only. **REMEMBERED**

---

## 7. Defaults and sanctioned overrides

We run every engine as shipped, and override a default only where leaving it would make the comparison meaningless. Four sanctioned categories:

1. **Resource fitting** (always applied): heap, thread pool and memory settings that fit an engine to the cell's cpuset and memory envelope. Envelope equality, not tuning.
2. **Vendor settle step**: documented bulk-load-then-query preparation, timed inside build (section 2).
3. **Operating-point matching**: where defaults put engines at different points of a quality/latency tradeoff, move them onto one point, because a latency comparison at unequal recall compares nothing.
4. **Documented escape hatch**: a default that is demonstrably pathological, tuned to the vendor's own recommendation, with the same care applied to every backend in the lane.

What we never do: per-system expert tuning beyond vendor guidance, or tuning ArcadeDB with insider knowledge not applied to comparators.

An override nobody can find is a defect, whichever way it moves the number. Every override is stamped on the row where a field exists (`es_prune`, `degree_param`, `degree_family`, `heap`, `server_heap`) **and** named in reader-facing text (table caption or page condition). `DISCLOSED WHERE = NOWHERE` marks work still to do.

| Engine | Setting | Category | Why | Disclosed where |
|---|---|---|---|---|
| ArcadeDB dense (emb + srv) | `LSM_VECTOR METADATA {"addHierarchy": true}` (default false) | operating-point | Makes the graph structurally comparable to the hierarchical hnswlib family. Inconsistent with our own E2 lane, which builds flat. | **NOWHERE**, and not stamped on any row |
| ArcadeDB server (all 4 arms) | `queryMaxHeapElementsAllowedPerOp=5000000` | escape hatch | Lets a >1.24M-group top-N complete. The default auto-scales to `max(500k, heap/2048)`, so the pin is LOWER than default at the 12g, 16g and 24g tiers and higher at 4g/6g/8g. | **NOWHERE** (code comment only) |
| ArcadeDB embedded (L1 tabular only) | same knob, `jvm_args` | escape hatch | Applied in 1 of the 8 ArcadeDB embedded adapters, so E4 compares two different query-memory caps. | **NOWHERE** |
| QuestDB (L4) | WAL-apply poll runs as a settle step after the timer | settle step | No engine settles inside the timer. | l4 page condition |
| ArcadeDB native TS vs QuestDB/DuckDB | ingest timer stops at `wait_completion()` (accepted, sealing outstanding) | operating-point | Three ingest rates timed to three completion semantics. | **NOWHERE** |
| ArcadeDB native TS | `SHARDS 4` (default 0 = 11 under this cpuset) | resource fitting | Fixes the shard count instead of deriving it from the cpuset. | raw JSON only |
| ArcadeDB native TS | `TS_PRIMITIVE=1` (lane default 1); `TS_NUMPY` already defaults to 1 | escape hatch | One opt-in fast path under the native time-series headline; comparators use ordinary bulk paths. `TS_NUMPY=0` exists only to reproduce the pre-fix number. | row field `ts_path`, page row label 'native time series' |
| DuckDB (L1 tabular) | `PRAGMA threads = len(sched_getaffinity(0))` | resource fitting | The F6 fix. Applied in 1 of 4 DuckDB lanes; l1_tpc, l3d and l4 still run about 1.7x oversubscribed. | FAIRNESS.md F6; **NOWHERE** reader-facing |
| DuckDB (L1 tabular) | drop indexes before bulk load, recreate after | escape hatch | Idiomatic columnar bulk load, one arm only. | **NOWHERE** |
| DuckDB VSS (L3d) | `hnsw_enable_experimental_persistence=true` | escape hatch | Required to persist an HNSW index at all. The row is measured on a vendor-labeled experimental configuration. | **NOWHERE** |
| ArcadeDB (L1-TPC) | ArcadeDB, SQLite, MongoDB, SurrealDB, and ArangoDB index `l_shipdate` (a persistent index in `l1_tpc.py`); DuckDB and PostgreSQL get none | operating-point | Both published TPC-H queries filter that column. Bias runs for us; PostgreSQL would benefit most. | **NOWHERE** |
| Elasticsearch (L3s) | `xpack.security.enabled=false` | escape hatch | Harness plumbing; also removes TLS and auth from every ES latency measurement. | **NOWHERE** |
| Elasticsearch (L3s) | `number_of_replicas=0` | resource fitting | A single-node cluster cannot allocate a replica; the index would sit yellow forever. | **NOWHERE** |
| composed Qdrant (E2) | `location=":memory:"` while ArcadeDB runs on disk | **unsanctioned**, still live | The composed stack's vector half is the last in-memory arm. qDT re-ran the arm on 2026-09-16 and moved its Neo4j half to 2026.07.1, but it did not change the store: `e2_hybrid.py` still opens `QdrantClient(location=":memory:")`, and putting it on disk is October's. Every SurrealDB arm now runs on disk. | page table note |
| sqlite-vec (L3d) | no PRAGMA cache/mmap/journal settings at all | resource fitting (absent) | The one dense arm given no resource fitting: stock ~2 MB page cache inside a 36 GiB cap. October puts it on the SQLite arms' PRAGMAs (DECISIONS #74 item 5). | partly (the page discloses exact scan only) |
| Neo4j (L2, E2) | heap pinned per tier; `server_memory_pagecache_size` = container mem less heap less a 1 GiB reserve, floored at 512m | resource fitting | Heap parity, with the rest of the cap fitted for Neo4j rather than left at its default. | **NOWHERE** (runner comment only) |
| PostgreSQL (`postgres_tuned`) | shared_buffers etc derived from the container cap | resource fitting | Ablation arm asking whether image defaults make the latency comparison unfair. Published beside the default arm. | page rows |
| every engine with a durability setting, from 2026-10 | the relaxed commit class, matched: ArcadeDB `txWalFlush=0` (its default, kept), SQLite and sqlite-vec WAL with `synchronous=NORMAL`, the PostgreSQL family (postgres, postgres_tuned, pgvector, PG+AGE, TimescaleDB) `synchronous_commit=off`, MongoDB's timed writes at `w=1, j=false`, and ArangoDB, QuestDB, and SurrealDB embedded at their own defaults | operating-point, user decision (DECISIONS #81) | A commit returns without waiting for the disk and the log is flushed by the engine's own background policy. It is ArcadeDB's default and a documented production mode for each of the others, so the comparators are matched to us on a knob that costs them nothing to set. Neo4j, LadybugDB, and DuckDB have no setting to relax and are named on their own tables; SurrealDB served 3.2.4 has none to set and is in neither class. | row field `durability`, FAIRNESS.md F10, generated page condition |
| SQLite (documents, TPC, time series) | `PRAGMA foreign_keys=ON; journal_mode=WAL; synchronous=NORMAL` | operating-point, user decision (DECISIONS #70) | The defaults fsync twice per commit; WAL+NORMAL is the common production setting. Makes SQLite faster on the write rows, so the knob runs against us. | page durability note, COMPARATORS.md |
| harness-wide | the manifest records cpuset/mem/heap/images and no engine configuration | **unsanctioned** | Nothing records the overrides above in the artifact; this table is the only inventory. | **NOWHERE** |
| ArcadeDB dense, E2 | `"similarity": "EUCLIDEAN"` (default COSINE) | operating-point | SIFT1M ships exact L2 ground truth, DEEP-10M is unit-normalized, comparators are set to L2. Correct and symmetric. | lane docstring only |
| LanceDB (L3d) | `.ef(100).nprobes(10)` | operating-point | Puts LanceDB on the lane's shared operating point (unset ef gave recall 0.711); nprobes 10 from a flat sweep. | adapter comments only |
| all dense arms | `EF_CONSTRUCTION=100`, `EF_SEARCH=100` | operating-point | Shared beam. No-op for ArcadeDB; moves some comparators down from their own defaults, and no audit of those defaults exists. | lane docstring only |
| ArcadeDB server (all arms) | `ARCADEDB_OPTS_GC=` is cleared explicitly, and `JAVA_OPTS` carries `-XX:+UseCompactObjectHeaders` | operating-point | Makes E4 a transport comparison. Upstream keeps the GC flags in their own variable, `ARCADEDB_OPTS_GC`, precisely so they survive a `JAVA_OPTS` override, so clearing it is what puts both arms on one collector. | DECISIONS #54, enforced by `verify_pair_c25.sh` |
| ArcadeDB server vs embedded | both arms run ONE set of upstream jars on ONE JVM (Corretto 25) | fairness | The axis isolates transport, not JVM major, libc, collector, or object-header layout. `-XX:+UseCompactObjectHeaders` is rejected by JDK 21, so a Temurin 21 served arm could never have had what the embedded JVM gets. | DECISIONS #54, enforced by `verify_pair_c25.sh` (jars AND JVM major on both sides) |
| ArcadeDB server (all arms) | `ARCADEDB_OPTS_MEMORY=-Xms{heap} -Xmx{heap}` (image ships 2G) | resource fitting | Heap parity per tier. The 2 GB default cost the server 15% p50 and 35% build time at 10M. | T5 caption, FAIRNESS.md F3, `server_heap` read back |
| Elasticsearch (L3s) | `ES_JAVA_OPTS=-Xms{heap} -Xmx{heap}` | resource fitting | Envelope equality across the lane's JVM engines. | FAIRNESS.md F3, enforced by `observe_server` |
| all served backends | server takes the full tier cap, client a separate `BENCH_CLIENT_MEM` (default 8g), stamped `mem_split="full+client"`; heap 50% of cap (67% at deep10m) | resource fitting | A served engine sees exactly the cap an embedded engine of the same tier sees, with the driver on top. Every frozen served row carries full+client; `BENCH_SERVER_MEM_FRACTION` restores the old 0.75/0.25 split for a reproduction. | FAIRNESS.md F3 |
| ES, Milvus, Qdrant, ArcadeDB, Neo4j, LadybugDB | forcemerge / flush+load / green-wait / COMPACT INDEX / awaitIndexes / CHECKPOINT, inside build | settle step | Each engine's own documented preparation. ArcadeDB takes none on L2 OLTP, and in the dense lane only Qdrant and Milvus define one at all. | disclosed as a class: FAIRNESS.md, page conditions |
| Milvus dense (L3d) | `dataCoord.segment.sealProportion=0.5` (image default 0.12) via a mounted `milvus.yaml` that differs from the image's in that one line; then flush, wait for the HNSW index, a compaction pass that is a no-op by then, load, all inside the timed build | resource fitting + settle step | The image seals growing segments at 12% of 1024 MB, so a 10M ingest lands as 26-28 segments that Milvus's own compaction merges at an unpredictable moment and a manual `compact()` cannot force (BUGS.md F8). Sealing at 50% produces the merged layout directly, so every run starts in the steady state. Applied to fp32 and INT8; the sparse lane is not bimodal and stays at the image default. | here and in the page condition; `milvus_segments_trace` on every row |
| ArcadeDB (L2 OLAP) | Graph Analytical View, `UPDATE MODE OFF`, build counted; `BENCH_GAV=0` ablation published | operating-point | ArcadeDB's documented OLAP answer, priced separately, with the view-off arm published. | page conditions, `gav` on every row |
| ArcadeDB (L3s) | int8 posting quantization (the engine default); fp32 arm prices it | operating-point | The headline arm is the default. The ablation is the only quantization axis available. | T4 caption, `SPARSE_PRECISION` |
| ArcadeDB dense (L3d), single-pass campaign rows | `graphBuildCacheSize=0`, the engine default: HNSW build cache sized as 25% of *available* heap at build start; unread for int8, which gets `DEFAULT_CACHE_SIZE=100,000` | operating-point (default) | The default is kept because it is what a user gets, and it cuts against us: on deep10m the served fp32 arm sized to 3.67M of 9.99M and built in 190-198 min where the same jars embedded sized to the whole corpus and built in 38-42; at a matched capacity the two are within 10%. Priced by the 21-cell ablation `results/ablation_cache_8d6af9475.jsonl`, which is operator and upstream material only (DECISIONS #55). | PAGE-SPEC dense build-cache note |
| ArcadeDB dense fp32 arms, the multipass overlays the page's dense table reads | `graphBuildCacheSize=9,990,000` (whole corpus), explicit; **INT8 arms at the engine default 100,000** | operating-point, **flatters us**, user decision (DECISIONS #56) | Pinning the cache to the corpus takes the served fp32 build from ~190 min to ~56. INT8 is left at its default because a pinned 3.67M bought only 12% there. Comparators have no such knob. | T5 caption and the page condition; #7146 documents the default's cost |
| ArcadeDB vs dense comparators | `M=32` (maxConnections) against `M=16` (doubled at base) | operating-point | Numerically equal settings build graphs of half the degree, so the lane matches effective base degree. 32 is also the engine's own default, so only the comparator side is set. | T5 caption, page, per-row stamps, FAIRNESS.md F7 |
| ArcadeDB dense (L3d, E2) | `neighborOverflowFactor` left at the engine default 1.2 | not an override, declined deliberately | Raising it to 2.0 cuts graph-unreachable nodes 3.5x with build time, peak RSS and recall flat. Declined because no hnswlib-family comparator has the knob, so it would move only our graph, in our favour. | FAIRNESS.md F7, DECISIONS #45 |
| Elasticsearch (L3s) | `index_options {"prune": false}` | operating-point | ES 9.1+ prunes on ELSERv2 thresholds; on SPLADE that costs the recall in section 1, and the default's bias ran in our favor. | page condition, `es_prune` on every row |
| ArcadeDB native TS | `TS_TAGS=1` reduced schema; unbounded last-point query | operating-point | Applied to all three engines. The published unbounded form is the faster one for us: 0.720 ms against 0.860 ms for the 1-hour window, measured as a pair in the same rep. | page schema-fidelity note, which states both numbers |
| Milvus | `DEPLOY_MODE=STANDALONE`, embedded etcd, vendor's own `embedEtcd.yaml` | resource fitting | Single-container deployment plumbing; the mounted file is Milvus's own default. | not stated, low severity |
| ArangoDB (L3d, E2) | FAISS IVF: `nLists = round(4*sqrt(n))`, `nProbe` calibrated in the cell against our own frozen fp32 recall | operating-point, matched by effect | The index has no degree, so F7's match is impossible and the operating point is matched on recall instead. | FAIRNESS.md F7, seven `ivf_*` fields on every row |
| Qdrant (L3s), ArcadeDB dense (L3d, E2) | sparse `on_disk=False`; dense `storeVectorsInGraph:false`, `beamWidth:100` | not an override | Explicit restatements of engine defaults, recorded so a future audit does not mistake one for a deviation. | recorded here only |
